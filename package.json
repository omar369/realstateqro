{
  "name": "realestateqro-landing",
  "private": true,
  "workspaces": [
    "packages/admin-panel",
    "packages/landing-page"
  ],
  "overrides": {
    "lucide-react": "^0.552.0"
  },
  "scripts": {
    "dev": "npm run dev -w landing-page",
    "build": "npm run build -w landing-page",
    "start": "npm run start -w landing-page",
    "lint": "npm run lint -w landing-page"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}

